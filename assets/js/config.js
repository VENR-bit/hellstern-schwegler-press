/* Edit this file — nothing else — to connect the signup form.
   Leave signupEndpoint as "" and the form falls back to opening the reader's
   email client, so the page still works before the backend exists. */
window.HSP = {
  // Google Apps Script web app URL, ending in /exec. See README.md.
  signupEndpoint: "",
  // Fallback + contact address.
  contactEmail: "hello@hellsternpress.com"
};
